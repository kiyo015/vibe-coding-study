# 基幹システム開発計画（2026-09-18〜）— 販売・購買・請求の基幹システムをAIと作る

2週間計画（`study_plan_2weeks.md`、Day1〜14）の続き。**Day15から通し番号を継続する。**

対象システムは `C:\Users\sakaguchi\sales-core`（GitHub: `kiyo015/sales-core`・private）。技術はC#（.NET 8）＋PostgreSQL 18。当面ひとりで開発するが、**後でチームに広げる前提で、最初からチーム用の形で作る**。

## 運用

- 平日のみ。日付は目安で、祝日や中断があればそのままずれる（番号の順序は変えない）
- **毎日の締め（「今日はここまで」）で、その日の作業と学びを`Study\YYYY-MM-DD\summary.md`＋`summary.html`に出力する**。手順は`daily-closeout`スキル
- 成果物リンクは両リポジトリとも同じタグ`day{N}`で固定する（`Study`と`sales-core`の両方にタグを打つ）
- 各日の終わりに、未完了の項目を翌日の先頭に書き写す。「洗い出して終わり」にしない

## 全体の構成（2026-09-18 確定）

機能範囲を広げ、5つの段階に分けて開発する（平日約78日）。設計の根拠は`sales-core/docs/specs/2026-09-18-scope-and-phases-design.md`、テーブル構造は`sales-core/docs/domain/er-diagram.md`。

| 段階 | Day | 日数 | 内容 | 終わった時点で動くもの |
|---|---|---|---|---|
| 1 | 15〜34 | 20 | 基盤と縦切り | APIで受注 → 出荷（分納あり） → **締め請求書の発行** |
| 2 | 35〜49 | 15 | 画面と権限（Blazor・ASP.NET Core Identity） | ログインし、役割に応じた画面で受注〜請求を操作できる |
| 3 | 50〜63 | 14 | 販売の完結 | 見積から入金・消込まで。売掛金の残高が出る |
| 4 | 64〜79 | 16 | 購買と在庫 | 発注から支払まで。在庫が無ければ出荷できない |
| 5 | 80〜92 | 13 | 帳票と外部連携 | 請求書などのPDF、会計ソフト向けの仕訳、銀行入金の取り込み |

**日単位の計画は各段階の最初に作る。** 段階1は日単位、段階2〜5は骨格のみ。各段階の最終日を振り返りにあて、次の段階を日単位に展開する。

業務上の決定（ER図に反映済み）: 締め請求＋出荷先と請求先の分離、分納あり、複数倉庫、得意先別単価あり（期間つき）。

## 基幹システムのルールを「お願い」から「強制」へ

| ルール | 強制する手段 | 予定日 |
|---|---|---|
| 金額に`double`・`float`を使わない | アナライザでビルドエラー | Day23 |
| 端数処理は`Money`の中だけ | アナライザ＋テスト | Day23 |
| 消費税は税率ごとに1回だけ端数処理（インボイス制度） | 請求書単位の計算をテストで固定 | Day21 |
| 受注・請求を物理削除しない | アーキテクチャテスト | Day27 |
| 未出荷のまま請求しない | 状態遷移を型で表す | Day22 |
| 接続文字列をファイルに書かない | `user-secrets`＋読み取り禁止ルール | Day15（済） |
| DBを壊すコマンドを打たない | `dev-guard`のフック＋`guard-e2e`で実効性確認 | Day17 |
| テストが落ちたまま進まない | `dev-guard`の編集後テスト（C#対応） | Day15（済） |

---

## 1週目：土台と安全柵

### Day15（2026-09-18 金）— 環境調査・骨格・C#対応 ✅ 完了

- 環境調査（.NET 8／PostgreSQL 18稼働中／Docker・LocalDBなし）、DB選定、テスト用DB方式の決定
- `sales-core`リポジトリ作成、6プロジェクト構成、CLAUDE.md、`.claude/settings.json`、`user-secrets`初期化
- GitHubのprivateリポジトリ作成とpush
- `dev-guard` 1.1.0：編集後の自動テストをC#に対応（対応する`.Tests`プロジェクトだけ走らせる。実測12.3秒）

**完了条件:** C#ファイルを編集するとフックが対応するテストを走らせ、失敗すれば差し戻す ✅

**実測値:** `dotnet test` 全体＝初回1分29秒・変更なし24.0秒／対応するテストプロジェクトだけ＝編集直後12.3秒。フックの実物確認＝成功9.1秒・失敗16.8秒で差し戻し。

**予定と違う点:** 予定の作業に加えて、計画の日単位化と締め手順の2リポジトリ対応、機能範囲の拡大（5段階・画面はBlazor・ログインはIdentity）、ER図（7枚）と仕様書の作成まで行った。ER図と計画を並べて、倉庫と得意先別単価の段階の割り当ての食い違いを2つ見つけて直した。

### Day16（2026-09-24 実施）— 要件の骨格 ✅ 完了

**持ち越し（Day15から）:** なし。ER図の「まだ決めていないこと」5点を、この日の業務ルール集で決める。

- 業務フロー（受注 → 出荷 → 請求）と各段階の状態を図と表で書く
- 用語集（受注・出荷・売上・請求・締め日・赤伝など）。**AIが言葉を取り違えないための定義**
- 業務ルール集の初版（税率10%・8%、端数処理の方式、**締め日の種類と締め後の訂正の扱い**、請求先の決め方、売上計上の基準、取消の扱い）
- ER図の「まだ決めていないこと」をここで決める
- `docs/domain/`に置き、CLAUDE.mdから`@docs/domain/...`で参照する
- 「決めなかったこと」も同じ文書に書く

**完了条件:** 新しいセッションで「請求書の端数処理はどうする？」と聞くと、業務ルール集の内容で答えが返る ✅

**実測値:** 新しいセッション（ヘッドレス）で3問確認し、業務ルール集どおりに回答した。

> 消費税：税率ごとに1回、四捨五入。明細ごとには行わない（インボイス要件）／売上計上：出荷確定日／締め日：得意先ごと選択（5・10・15・20・25日・月末）

作成した文書の行数＝`business-rules.md` 102行、`glossary.md` 60行、`business-flow.md` 78行、`er-diagram.md` 558行（`supplier_invoices`と照合表を追加）。Mermaidの図は11枚（業務フロー4・ER図7）すべて構文エラーなく描画。CLAUDE.mdからは`business-rules.md`だけを`@`で常時読み込み、残り3つはパス参照（常時読み込みは文脈を消費するため）。

**確定した8点:** 消費税は四捨五入（税率ごとに1回）／売上は出荷基準／締め日は得意先ごと（5・10・15・20・25日・月末）／締め後の訂正は翌月に赤黒／受注の承認は税抜100万円以上（基準額は設定で保持）／仕入先請求書は照合してから支払う／単価は小数2桁／数量は小数3桁。派生して「明細金額は円単位で四捨五入」も決めた。

**予定と違う点:** ヘッドレス実行で `Ignoring 6 permissions.allow entries from .claude/settings.json: this workspace has not been trusted.` が出て、`sales-core`の許可ルールが無視されていることが分かった。本人が信頼を承認して解消済み（`hasTrustDialogAccepted: true`、再実行で警告なし、`git status`実行時の`permission_denials`が空）。

### Day17（2026-09-25 実施）— DBを壊すコマンドのガード ✅ 完了

- `pre-bash-guard`に追加（TDDで）: `dotnet ef database drop`、`dotnet ef migrations remove`、SQLの`DROP`・`TRUNCATE`
- `guard-e2e`に`sales-core`向けのセルを追加: DB破壊コマンド、秘密情報ファイルの読み取り禁止
- 対照実験（ガードを外すと不合格になること）まで確認する

**完了条件:** 追加した2セルが対照実験付きで合格し、`guard-matrix.md`に反映されている ✅

**実測値:** ガードのテストは46件 → 67件（追加21件＝止める10・通す11）、全件成功。RED時は止める10件だけが`false !== true`で落ちることを確認した。ガード実効性テストは5セルすべて「ガードあり: blocked-by-guard ／ ガードなし: executed」で合格、1回の実行費用$0.3786。判定ロジックのテストは19件（`classifyDeny`の4件を追加）。`dev-guard`を1.2.0に更新。

**追加した検知:** `dotnet ef database drop`、`dotnet ef migrations remove`、SQLの`DROP`（table/database/schema/index/view/column/constraint/type/sequence）と`TRUNCATE`。語の境界で見るので`dropped_records`や`drop_old.sql`には反応しない。`dotnet ef migrations add`・`database update`・`list`・`script`は通す。

**判定の工夫:** denyルールのセルは「拒否の記録があるか」ではなく「ファイルに仕込んだ目印が返ってこないこと」を主な証拠にした（`permission_denials`は別のファイルの拒否でも埋まるため）。対照実験は設定を差し替えず、`.env`（deny対象）と`guard-probe-secret-7f3a.txt`（対象外）でファイル名だけを変え、差をルール1つに絞った。

**予定と違う点:** 対照実験でDB破壊コマンドを実際に実行させる必要があるため、先に`dotnet ef --version`で`dotnet-ef`が未インストールであることを確認してからセルに入れた。また`node -e`経由でバックスラッシュが消える失敗（Day15・Day16に続き3日連続）が再発し、正規表現の`\b`（語の境界）が0x08の制御文字に化けた。1行はEdit、複数行はスクリプトをファイルに書いてから実行する手順に固定した。

**持ち越し:** このDB破壊ガードは`Study`で作業している時しか効かない（`sales-core`にプラグイン未接続）。Day18の作業そのものなので、そちらで解消する。

### Day18 — プラグインの配布とユーザー全体設定

- `dev-guard`を`sales-core`から使えるようにする。**絶対パス参照は避ける**方法を決める（独立リポジトリに出す／git URLのマーケットプレイスにする、のいずれか）
- `sales-core`で有効化し、フックが実際に発火することを確認する
- `~/.claude/CLAUDE.md`を作り、個人の好み（口調・言語）をプロジェクトから分離する（弱点B）

**完了条件:** `sales-core`側の設定に絶対パスが無く、フックが発火する。プロジェクトのCLAUDE.mdに個人の好みが残っていない

### Day19 — DB接続とテスト用DBの実証、週次振り返り

- `user-secrets`に接続文字列を置き、EF Core（Npgsql）を導入。空のマイグレーションを1本通す
- テスト専用DBを作り、結合テストが**トランザクションのロールバックで独立する**ことを実証する
- 1週目の振り返り。未完了を2週目の先頭に書き写す

**完了条件:** 結合テストを2回連続で実行しても、DBの状態が積み上がらない

---

## 2週目：業務ルールの中核（DBを使わない層）

### Day20 — 金額型と端数処理

- `Money`型（`decimal`のみ）。端数処理は業務ルール集の方式に従い、`Money`の中だけで行う
- 境界値（ちょうど0.5円など）のテスト

**完了条件:** 端数処理のテストが境界値を含めて通る。丸めの実装が1か所にしかない

### Day21 — 消費税

- 税率ごとに1回だけ端数処理する（適格請求書の要件）。軽減税率8%の混在に対応
- **ランダムな明細を大量に作り、「明細の合計と請求額が常に一致する」ことを確かめる**

**完了条件:** 税率混在・端数の出る金額で、請求書単位の税額が合う

### Day22 — 受注の状態遷移

- 受注 → 出荷 → 請求の遷移を型で表す。飛び越し・逆戻りを不可能にする
- **分納**：受注明細の出荷済み数量で「一部出荷」「出荷済」を判定する。受注数を超える出荷を不可能にする
- 取消は状態として表す（削除しない）

**完了条件:** 未出荷のまま請求しようとするコードが、コンパイルかテストで落ちる

### Day23 — アナライザで強制にする

- BannedApiAnalyzersで`double`・`float`の金額利用、`Money`以外での`Math.Round`を禁止
- **わざと違反するコードを書き、ビルドが落ちることを確認する**

**完了条件:** 違反コードでビルドが落ちる。CLAUDE.mdの該当ルールが「お願い」から「強制」に変わっている

### Day24 — ルール変更手順のスキル化とレビュー役の強化

- `domain-rule-change`スキル: 税率・端数処理・締め日を変える時に、コード・テスト・`docs/domain/`を必ず一緒に直す手順
- `team-reviewer`の観点に「金額の型と端数処理」「状態遷移」「取消と削除」「秘密情報」を追加
- **model比較（弱点B）:** わざと端数処理の誤りを仕込んだ差分を、haiku／sonnet／opusにレビューさせ、検出数・誤検知・費用・時間を表にする

**完了条件:** 比較表ができ、実測に基づいてレビュー役の`model`を決めている

---

## 3週目：永続化とCI

### Day25 — EF Coreでの永続化とマイグレーション手順

- 段階1で使う表だけをマッピングする：得意先（**請求先・締め日を含む**）、商品、税区分・税率、倉庫（1件）、受注・出荷・売上・請求とその明細、監査ログ
- `migration`スキル（生成 → **SQLスクリプトを出力してレビュー** → 開発DBにだけ適用）

**完了条件:** マイグレーションのSQLをレビューしてから適用する手順が、スキルとして回る

### Day26 — 結合テスト

- 受注 → 出荷 → 請求をDB込みで通すテスト。テスト専用DBで独立実行

**完了条件:** 結合テストがCIでも手元でも同じ結果になる

### Day27 — 楽観ロック・監査ログ・削除の禁止

- 同時更新の衝突検知、誰がいつ何を変えたかの記録
- アーキテクチャテストで受注・請求の物理削除を禁止する

**完了条件:** 物理削除するコードを書くとアーキテクチャテストが落ちる。衝突がテストで再現できる

### Day28 — CI（ビルドとテスト）

- GitHub Actionsで`dotnet build`・`dotnet test`。テスト用DBの用意もCI上で行う

**完了条件:** PRを作るとCIでテストが走り、失敗すればマージできない

### Day29 — CI（AIレビュー）

- `claude -p`によるPRレビュー。Day12・13・14の教訓をそのまま使う（プロンプトは標準入力／`--tools ""`と`--strict-mcp-config`／失敗の基準も渡す／1回の費用を記録）
- **APIキーの登録は本人が行う**

**完了条件:** PRにAIレビューの結果が残り、1回あたりの費用が記録されている

---

## 4週目：縦切りの完成と振り返り

### Day30 — 受注・出荷API

**完了条件:** 受注登録と出荷登録がAPI経由で通り、状態が正しく遷移する

### Day31 — 締め請求書の発行APIと端から端までのテスト

- 請求先・締め日の単位で、期間中の売上をまとめて請求書を作る。税率ごとの集計を持つ
- 締め済みの売上を二重に請求しない

**完了条件:** 受注から分納での出荷、締め請求書の発行までを1本で通すテストがCIで通る。金額が2週目のDomainのテストと同じ規則で計算されている

### Day32 — hooksの他イベントと健康診断のC#対応

- `Stop`（テスト未実行・未コミットの通知）、`PreCompact`（その日の業務判断を`docs/decisions/`へ退避）。stdinで受け取るデータを実測してから作る
- 健康診断に`dotnet test`・未適用のマイグレーション・脆弱性のあるパッケージを追加

**完了条件:** 2つのイベントの発火を実測した記録がある。健康診断がC#の項目込みで無人完走する

### Day33 — ガードの定期実行とWindowsの整理

- `guard-e2e`を週1回の定期実行に組み込む
- Windowsの落とし穴を整理し、未確認の3件（孫プロセス、PowerShell用のdenyルール、`decision: 'block'`の非推奨）に結論を出す

**完了条件:** `guard-matrix.md`に未検証セルが無く、週1回の実行が無人で完走する

### Day34 — 総振り返りとチーム展開の準備

- 段階1の振り返り。`templates/team-CLAUDE.md`を基幹システム版に更新
- 新しく参加した人が初日に安全柵の動作を確認できる手順を書く
- **段階2（画面と権限）を日単位に展開する**

**完了条件:** 段階2の計画が、この形式（日単位・完了条件つき）で書けている

---

## 段階2：画面と権限（Day35〜49・15日）— 骨格

Day34で日単位に展開する。

- `src/SalesCore.Web`にBlazor Web App。**画面は薄く保つ**（業務の判断はAPIとDomain、画面は表示と入力だけ）
- ASP.NET Core Identity（パスワード規則・ロック・ハッシュ化は標準機能を使う）
- 役割（営業・経理・管理者）と、APIごとの認可ポリシー。監査ログに操作した人を記録
- 受注の承認（一定金額以上は管理者の承認まで出荷不可）
- 画面：マスタ（得意先・商品・倉庫）、受注・出荷・締め請求（明細の入力を含む）
- テスト：bUnit（部品）、Playwright（端から端まで）
- **権限の実効性テスト**：「営業が経理のAPIを呼ぶと拒否される」を、権限を外した場合と比べて確かめる

**段階の完了条件:** 3つの役割それぞれでログインし、許された操作だけができる。権限の実効性テストが対照実験付きで合格している

## 段階3：販売の完結（Day50〜63・14日）— 骨格

見積、得意先別単価（期間つき）、入金、消込（入金と請求の突き合わせ）、売掛金の残高、月次締め。

**段階の完了条件:** 見積から入金・消込までが通り、月次締めで売掛金の残高が確定する

## 段階4：購買と在庫（Day64〜79・16日）— 骨格

仕入先・仕入単価、発注 → 入荷・検収 → 仕入 → 支払・買掛金、在庫（商品×倉庫、入出庫の履歴、引当、棚卸）。出荷と在庫を連動させる。

**段階の完了条件:** 発注から支払までが通り、在庫が足りない受注は出荷できない。在庫数が入出庫の履歴から再計算できる

## 段階5：帳票と外部連携（Day80〜92・13日）— 骨格

PDF（請求書・納品書・発注書、発行履歴と再発行の管理）、会計ソフト向けの仕訳データ出力、銀行入金データ（全銀協形式）の取り込みと自動消込。

**段階の完了条件:** 請求書のPDFが発行でき、仕訳データが会計ソフトに取り込める形式で出力され、銀行データから入金と消込が自動で作られる

---

## 日々の記入欄

各日の実施後に、`daily-closeout`がこの欄を埋める。

| Day | 日付 | 主な内容 | 完了 |
|---|---|---|---|
| 15 | 2026-09-18 | 環境調査・sales-core骨格・dev-guardのC#対応・計画の日単位化・機能範囲の拡大（5段階）・ER図 | [x] |
| 16 | 2026-09-24 | 要件の骨格（業務ルール集・用語集・業務フロー・ER図の確定／保留8点を決定／ワークスペース信頼を解消） | [x] |
| 17 | 2026-09-25 | DB破壊コマンドのガード（TDDで検知を追加／E2Eに2セル追加し5セル合格／guard-matrix更新） | [x] |
| 18 | | プラグイン配布・ユーザー全体設定 | [ ] |
| 19 | | DB接続とテスト用DB・週次振り返り | [ ] |
| 20 | | 金額型と端数処理 | [ ] |
| 21 | | 消費税 | [ ] |
| 22 | | 受注の状態遷移 | [ ] |
| 23 | | アナライザで強制化 | [ ] |
| 24 | | ルール変更スキル・レビュー役・model比較 | [ ] |
| 25 | | EF Coreとマイグレーション手順 | [ ] |
| 26 | | 結合テスト | [ ] |
| 27 | | 楽観ロック・監査ログ・削除禁止 | [ ] |
| 28 | | CI（ビルドとテスト） | [ ] |
| 29 | | CI（AIレビュー） | [ ] |
| 30 | | 受注・出荷API | [ ] |
| 31 | | 請求書発行APIと端から端までのテスト | [ ] |
| 32 | | hooksの他イベント・健康診断のC#対応 | [ ] |
| 33 | | ガードの定期実行・Windowsの整理 | [ ] |
| 34 | | 総振り返り・チーム展開の準備 | [ ] |
