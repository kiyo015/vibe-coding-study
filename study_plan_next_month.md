# 次月ロードマップ（2026-10）

2週間計画（`study_plan_2weeks.md`）の振り返り（Day14）で特定した「まだ弱い」領域3つを、1か月で潰す計画。

使い方: 2週間計画と同じく平日のみ・日別フォルダに`summary.md`＋`summary.html`、締めは`daily-closeout`スキル。**各テーマは「理解した」でなく、下の完了条件を実測で満たした時点で完了とする。**

---

## 出発点（2週間で到達した地点）

- 5機構（CLAUDE.md・スキル・サブエージェント・hooks・MCP）とプラグインを、コスト・反映タイミング・強制力で使い分けられる
- 「お願い」と「強制」を区別してチーム向けのルールを書ける（`templates/team-CLAUDE.md`）
- 読み取り専用のレビュー役で指摘を受け、実測してから直す・反論する流れが回っている
- 健康診断を2つのスケジューラで毎日無人実行している（正常時コスト0）

---

## テーマA：ガードの実効性テスト

### なぜ弱点と判断したか

記録に「効いていなかった」が3日にわたって出てくる。**どれも設定した時点では確認できず、別の作業の途中で偶然見つかった。**

| Day | 効いていると思っていたもの | 実態 |
|---|---|---|
| 7 | worktreeによる隔離 | git未追跡のファイルは素通し |
| 11 | 危険コマンドのフック・denyルール | PowerShellツールには一切効いていなかった |
| 13 | 無人実行の`--tools ""` | Day12から一度も効いていなかった（cmd.exeで引数が消えていた） |

問題は個々の設定ミスではなく、**「設定した」と「効いている」の間を確かめる仕組みが無い**こと。

### やること

1. **経路×ガードの表を作る。** 経路（Bashツール／PowerShellツール／ヘッドレス`claude -p`／サブエージェント／定期タスク）と、ガード（denyルール／`dev-guard`フック／サブエージェントの`tools`／`--tools`）の組み合わせを列挙し、各セルで「効くべきか」を決める
2. **端から端まで通す検証を書く。** 単体テスト（フックに入力を渡して判定を見る）ではなく、本物のClaude Codeに無害な「危険に見える」操作（存在しないパスへの`rm -rf`など）を試させ、ブロックされたかを`permission_denials`や出力で判定する
3. **ガードを意図的に外すと赤になる**ことを確認する（Day13の教訓：狙った理由で落ちることまで見る）
4. **健康診断に組み込む。** 1回ごとに費用がかかるので毎日ではなく週1回にし、1回あたりの費用を記録する

### 完了条件

- 表の全セルが自動で「効いている／効いていない」と判定される
- どのガードを外しても、対応するセルが赤になる
- 週1回の自動実行が、人の手なしで2回連続完走する

---

## テーマB：未実践の3機構＋CI

### なぜ弱点と判断したか

Day7の振り返りで「未実施」とした3つが、その後の6日間で一度も扱われなかった。Day11の予告にあった「CIへの組み込み」も未実施。**洗い出しただけで、持ち越しの管理ができていなかった。**

| 未実施のもの | 洗い出した日 |
|---|---|
| サブエージェントのmodel指定の効果 | Day7 |
| hooksの他のイベント（`Stop`・`SubagentStop`・`PreCompact`） | Day7 |
| ユーザー全体用のCLAUDE.md（`~/.claude/CLAUDE.md`） | Day7 |
| CIへの組み込み | Day11の予告 |

### やること

1. **model指定を比較する。** `team-reviewer`と`game-playtester`に、haiku／sonnet／opusで同じ課題を与える。比べるのは、指摘の的中数（Day11・13と同じく実測で判定）・費用・時間。「どの役にどのモデルが割に合うか」を表にする
2. **hooksの他のイベントを使う。** 候補は、`Stop`で未コミットの変更を知らせる、`PreCompact`で要約前に会話の要点を退避する、`SubagentStop`でサブエージェントの出力を記録する。それぞれ発火時に受け取るデータをstdinダンプで実測する（Day6と同じ手法）
3. **ユーザー全体用のCLAUDE.mdを設計する。** 個人の好み（日本語で書く・報告の形式など）をプロジェクトから切り離す。`templates/team-CLAUDE.md`との役割分担を決める
4. **CIに組み込む。** GitHub Actionsで`claude -p`を動かす（PRレビューか健康診断）。秘密情報の置き場所・権限・費用の上限を設計する。**APIキーの登録は本人が行う**

### 完了条件

- model比較の表（役割×モデル×的中数・費用・時間）ができ、各サブエージェントの`model`を実測に基づいて決め直している
- 3イベントそれぞれで、発火と受け取るデータを実測した記録がある
- `~/.claude/CLAUDE.md`が存在し、プロジェクトのCLAUDE.mdから個人の好みが消えている
- PRを作るとCIで`claude -p`が走り、結果がPRに残る

---

## テーマC：Windows実行環境の整理

### なぜ弱点と判断したか

シェルの境界で5回つまずき、**どれも初見で、つまずいてから知った。**

| Day | つまずき |
|---|---|
| 10 | Git Bashの`printf`でWindowsパスの`\\`が崩れ、テスト入力のJSONが壊れた |
| 11 | 同じフォルダが`C:/…`と`C:\…`の2通りで記録され、片方だけ未信頼だった |
| 12 | `.cmd`の日本語コメントをcmd.exeがShift-JISとして読み、`exit=9009` |
| 12 | タスクスケジューラから呼ぶとカレントディレクトリが想定と違う |
| 13 | `shell: true`で引数がエスケープされず連結され、プロンプトと`--tools`が消えた |

### やること

1. **落とし穴の一覧を作る。** 上の5件を「症状／原因／確かめ方／対策」で整理する
2. **確かめるスクリプトを置く。** 引数の渡り方（Day13の偽`.cmd`の手法）、文字コード、パス表記を、新しい環境で1回走らせれば分かる形にする
3. **チーム向けCLAUDE.mdの雛形に追記する**
4. **未確認のまま残った事実を解消する。**
   - タイムアウトで打ち切った後、孫プロセスが残るか（Day11）
   - PowerShellツール向けのdenyルールの書き方（Day11）
   - PreToolUseの`decision: 'block'`は非推奨か（Day11の指摘）

### 完了条件

- 5件すべてに、再現と対策を確かめるスクリプトがある
- 未確認の3件すべてに、実測の結論が記録されている
- 雛形に「Windowsで最初に確かめること」の節がある

---

## 週割り（目安）

| 週 | テーマ | 主な成果物 |
|---|---|---|
| 1週目 | A：ガードの実効性テスト | 経路×ガードの表、端から端まで通す検証、健康診断への組み込み |
| 2週目 | B：model指定の比較、hooksの他イベント | 比較表、3イベントの実測記録 |
| 3週目 | B：ユーザー全体用CLAUDE.md、CI | `~/.claude/CLAUDE.md`、GitHub Actionsのワークフロー |
| 4週目 | C：Windows実行環境の整理、総括 | 落とし穴の一覧と検証スクリプト、雛形への追記、1か月の振り返り |

**持ち越しの管理（テーマBの反省）:** 各週の終わりに、未完了の項目を次の週の最初の作業として計画に書き写す。「洗い出して終わり」にしない。
