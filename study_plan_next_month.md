# 次月ロードマップ（2026-10）— 販売・購買・請求の基幹システムをAIと作る

2週間計画（`study_plan_2weeks.md`）の総仕上げとして、**販売・購買・請求の基幹システムを新規に開発する**。題材を練習用から本番の開発に切り替え、Day14で特定した弱点3つ（A: ガードの実効性、B: 未実践の機構とCI、C: Windowsの実行環境）を、その開発の工程に組み込んで潰す。

使い方: 平日のみ。日別フォルダに`summary.md`＋`summary.html`、締めは`daily-closeout`スキル。**各週は完了条件を実測で満たした時点で完了とする。**

---

## 前提

| 項目 | 決定 |
|---|---|
| 対象業務 | 販売・購買・請求 |
| 技術 | C#（.NET）＋ RDB |
| 体制 | 当面ひとり。**後でチームに広げる前提で、最初からチーム用の形で作る** |
| 1か月のゴール | 「受注 → 出荷 → 請求書の発行」を1本通す縦切り。入金消込・購買は翌月以降 |

### 1か月では作らないもの（YAGNI）

- 画面（APIを先に作る。画面は縦切りが固まってから）
- 本格的な認証基盤（役割は最小限の2つで仮置き）
- 帳票のPDF出力、外部の会計ソフト連携、本番環境へのデプロイ

---

## この開発の原則（2週間の学びから）

### 1. 基幹システムのルールは「お願い」から「強制」へ移す

基幹システムは、ルール違反がそのまま金額の誤りや会計データの消失になる。Day11の「お願い（CLAUDE.mdに書くだけ）と強制（技術的に止める）」を、最初から設計に入れる。

| ルール | お願いのままだと | 強制する手段 | 週 |
|---|---|---|---|
| 金額に`double`・`float`を使わない | 端数の誤差が請求額に出る | Roslynアナライザ（BannedApiAnalyzers）で**ビルドエラーにする** | 2 |
| 端数処理は1か所でだけ行う | 画面と請求書で1円ずれる | 金額型（`Money`）以外での丸めをアナライザで禁止＋テスト | 2 |
| 消費税は税率ごとに1回だけ端数処理する（インボイス制度） | 明細ごとに丸めて合計が合わない | 請求書単位の計算をテストで固定 | 2 |
| 受注・請求のデータを物理削除しない（取消は赤黒処理か論理削除） | 監査で追えない | アーキテクチャテストで`Remove`呼び出しを禁止＋レビュー観点 | 3 |
| 状態を飛び越えない（未出荷のまま請求しない） | 請求漏れ・二重請求 | 状態遷移をドメインモデルの型で表し、不正な遷移はコンパイルかテストで落とす | 2 |
| 接続文字列・秘密情報をファイルに書かない | リポジトリに混入する | `dotnet user-secrets`＋`.env`・`appsettings.*.json`の読み取り禁止ルール | 1 |
| DBを壊すコマンドを打たない（`dotnet ef database drop`など） | 開発データの消失 | `dev-guard`フックに追加し、**`guard-e2e`で効いていることを確かめる** | 1 |
| マイグレーションを手元から本番に適用しない | 本番のスキーマ破壊 | CI経由のみ。手元のコマンドはフックで止める | 3 |

### 2. 業務ルールは「1つの定義元」に集める

Day13で難易度を`LEVELS`表1つに集めたのと同じ考え方。税率・端数処理・状態遷移の定義は、コードの1か所と`docs/domain/`の文書1つにまとめ、CLAUDE.mdから`@`で読ませる。変更手順は`game-balance`スキルと同じ形でスキル化し、「コード・テスト・文書を必ず一緒に直す」を手順として持たせる。

### 3. 仕様 → 計画 → テスト先行 → レビュー → 確認

Day13で通した流れを標準にする。基幹システムでは特に、**仕様書に「決めたこと」と「決めなかったこと」**、**計画にテストの期待値**を書いてから実装に入る。

### 4. 設定したら「効いているか」まで確かめる（弱点A）

新しく入れたガードは、その週のうちに`checks/guard-e2e.js`のセルとして自動化する。「効くはずだと思っているだけ」のセルを作らない。

---

## 1週目：土台と安全柵

**ゴール:** コードを1行も書かないうちに、秘密情報とDBが守られ、編集のたびにテストが走る状態を作る。

### やること

1. **要件の骨格を決める**（`brainstorming`スキル）
   - 業務フロー（受注 → 出荷 → 請求）と、各段階の状態
   - 用語集（受注・出荷・売上・請求・締め日など。AIが言葉を取り違えないための定義）
   - 業務ルール集の最初の版（税率10%・8%、端数処理の方式、締め日）
2. **技術の選定を記録する**（1ページの意思決定記録）
   - RDB（SQL Server か PostgreSQL か）
   - テスト用DBの方式。インメモリDBは本番と挙動が違う（制約・トランザクション）ので、**本物と同じDBをテストで使う方式**を優先して比べる
3. **リポジトリを作る**
   - **初日にprivateのリモートを作る**（`equipment-management`はリモートが無かった）
   - ソリューション構成: `Domain`（業務ルール。DBを知らない）／`Application`／`Infrastructure`（EF Core）／`Api`／テストプロジェクト
   - `.gitignore`に`.claude/settings.local.json`と秘密情報のファイルを入れる（Day11の漏れを繰り返さない）
4. **CLAUDE.mdを書く**（`templates/team-CLAUDE.md`を土台に）
   - ルールごとに「強制／お願い」を明記する
   - 業務ルール集・用語集を`@docs/domain/...`で読ませる
   - **ユーザー全体用のCLAUDE.md（`~/.claude/CLAUDE.md`）を作り、個人の好みをそちらへ分離する**（弱点B）
5. **安全柵を入れる**
   - `.claude/settings.json`: `dotnet build`・`dotnet test`の許可、秘密情報ファイルの読み取り禁止
   - **`dev-guard`をC#に対応させる**（Day10の教訓：まとめるのではなく汎用化する）
     - 編集後の自動テスト: 近い`.csproj`／`.sln`を探して`dotnet test`を走らせる。**先に所要時間を測る**（ビルドを含むので25秒の上限に収まらない可能性が高い。収まらなければ、対象のテストプロジェクトだけ・`--no-restore`で走らせる設計にする）
     - 危険なコマンド: `dotnet ef database drop`、`dotnet ef migrations remove`、SQLの`DROP`・`TRUNCATE`を追加
6. **Windowsの実行環境を確かめる**（弱点C）
   - `dotnet`コマンドの引数の渡り方・文字コード・パス表記を、Day13の偽`.cmd`の手法で最初に確認する

### 完了条件

- リモートにpushされ、`dotnet build`と`dotnet test`（空のテスト1件）がCLAUDE.mdに書いたコマンドで通る
- C#ファイルを編集すると`dev-guard`が`dotnet test`を走らせ、失敗すれば差し戻す
- `guard-e2e`に次のセルが追加され、**すべて対照実験付きで合格**している
  - 秘密情報ファイルの読み取り禁止
  - DBを壊すコマンド × `dev-guard`フック
- `~/.claude/CLAUDE.md`があり、プロジェクトのCLAUDE.mdに個人の好みが残っていない

---

## 2週目：業務ルールの中核（DBを使わない層）

**ゴール:** 受注から請求までの金額計算と状態遷移を、DBなしの`Domain`層でテストとともに完成させる。

### やること

1. **金額型（`Money`）と税計算**
   - `decimal`のみ。端数処理の方式（切り捨て・四捨五入）は業務ルール集から1か所で決める
   - 請求書は**税率ごとに1回だけ端数処理する**（インボイス制度の適格請求書）
   - テストは代表値に加え、**境界値（端数がちょうど0.5円など）と大量のランダムな明細**で「明細の合計と請求額が常に一致する」ことを確かめる（プロパティベーステスト）
2. **状態遷移**
   - 受注 → 出荷 → 請求の遷移を型で表し、飛び越し・逆戻りが起きないようにする
   - 取消は物理削除でなく、状態として持つ
3. **強制の層を入れる**
   - BannedApiAnalyzersで`double`・`float`の金額利用と、`Money`以外での`Math.Round`を禁止する。**わざと違反するコードを書いてビルドが落ちることを確認する**（狙った理由で落ちることまで見る）
4. **業務ルール変更のスキルを作る**（`domain-rule-change`）
   - 税率・端数処理・締め日を変える時の手順。コード・テスト・`docs/domain/`を必ず一緒に直す
5. **レビュー役を基幹システム向けにする**
   - `team-reviewer`の観点に「金額の型と端数処理」「状態遷移」「取消と削除」「秘密情報」を足す
   - **model指定を比較する**（弱点B）: haiku／sonnet／opusに、わざと端数処理の誤りを仕込んだ同じ差分をレビューさせる。比べるのは誤りを見つけた数・誤検知の数・費用。この結果でレビュー役の`model`を決める

### 完了条件

- `Domain`層のテストが、境界値とランダムな明細を含めて全件通る
- `double`で金額を扱うコードを書くとビルドが落ちる（実際に確認済み）
- レビュー役のmodel比較表があり、仕込んだ誤りの検出数に基づいて`model`を決めている

---

## 3週目：永続化とCI

**ゴール:** DBに正しく保存・更新でき、PRを出すとCIでテストとAIレビューが走る。

### やること

1. **EF Coreでの永続化**
   - マイグレーションの手順をスキル化する（`migration`）: 生成 → **SQLスクリプトを出力してレビュー** → 開発DBにだけ適用
   - 同時更新の衝突対策（楽観ロック）
   - 監査ログ（誰がいつ何を変えたか）
   - アーキテクチャテストで、受注・請求の物理削除を禁止する
2. **テスト用DB**
   - 1週目の選定どおり、本物と同じDBで結合テストを回す
3. **CIに組み込む**（弱点B）
   - GitHub Actionsで`dotnet build`・`dotnet test`
   - PRで`claude -p`によるレビューを走らせる。Day12・13の教訓をそのまま使う
     - プロンプトは標準入力で渡す
     - `--tools ""`に加えて`--strict-mcp-config`を付ける
     - 失敗したものの「基準」も一緒に渡す
     - 1回あたりの費用を記録する
   - **APIキーはGitHubのSecretsに本人が登録する**
4. **hooksの他のイベント**（弱点B）
   - `Stop`: 作業の終わりに、テストが未実行・未コミットの変更が残っていれば知らせる
   - `PreCompact`: 会話が要約される前に、その日の業務判断（なぜその端数処理にしたか等）を`docs/decisions/`へ退避する
   - それぞれstdinで受け取るデータを実測してから作る（Day6の手法）

### 完了条件

- 受注 → 出荷 → 請求の状態が、同時更新の衝突を含めてDBで正しく保存される（結合テストで確認）
- 受注・請求を物理削除するコードを書くと、アーキテクチャテストが落ちる
- PRを作るとCIでテストとAIレビューが走り、結果がPRに残る。1回あたりの費用が記録されている
- `Stop`・`PreCompact`フックの発火を実測した記録がある

---

## 4週目：縦切りの完成と振り返り

**ゴール:** 「受注 → 出荷 → 請求書の発行」をAPIで1本通し、次の月（購買・入金消込・チーム展開）に進める状態にする。

### やること

1. **縦切りを通す**
   - 受注登録 → 出荷登録 → 請求書発行のAPIと、それを端から端まで通す結合テスト
   - 請求書の金額が、2週目の`Domain`層のテストと同じ規則で計算されていることを確かめる
2. **健康診断をC#向けに広げる**
   - `dotnet test`、未適用のマイグレーション、脆弱性のあるパッケージ（`dotnet list package --vulnerable`）
   - `guard-e2e`を週1回の定期実行に組み込む（弱点A）
3. **Windows実行環境の落とし穴を整理する**（弱点C）
   - 1か月で踏んだものと、2週間計画の5件をまとめ、確かめるスクリプトを置く
   - 未確認のまま残っている3件（孫プロセス、PowerShell用のdenyルール、`decision: 'block'`の非推奨）に結論を出す
4. **振り返りとチーム展開の準備**
   - `templates/team-CLAUDE.md`を基幹システム版に更新する
   - 新しく加わった人が、初日に安全柵が効いていることを確かめられる手順（`guard-e2e`の実行）を書く

### 完了条件

- 請求書発行までの結合テストが、CIで通っている
- 健康診断がC#の項目を含めて毎日無人で完走し、`guard-e2e`が週1回合格している
- `guard-matrix.md`に未検証のセルが無い
- Windowsの落とし穴と未確認3件に、実測の結論が記録されている

---

## 弱点3つとの対応

| 弱点（Day14で特定） | 根拠 | この計画で潰す場所 |
|---|---|---|
| A. 設定したガードが効いているかの確認が後手 | 「効いていなかった」がDay7・11・13、Day14にも`--tools ""`でMCPが残っていた | 各週、新しいガードは同じ週に`guard-e2e`で自動化（1・4週目） |
| B. 洗い出したのに手を付けなかった機構 | Day7で未実施とした3つとCIが、その後一度も扱われなかった | ユーザー全体用CLAUDE.md（1週目）、model比較（2週目）、CIと他イベントのフック（3週目） |
| C. Windowsのシェル境界で繰り返しつまずく | Day10〜13で5件、すべて初見 | `dotnet`の引数確認（1週目）、落とし穴の整理と未確認3件の決着（4週目） |

**持ち越しの管理:** 各週の終わりに、未完了の項目を次の週の最初の作業として、この計画に書き写す。「洗い出して終わり」にしない（弱点Bの反省）。
