# CLAUDE.md（frontend配下）

このディレクトリはバックエンド（`../src`）と規約が異なる。作業時はこちらを優先すること。

## スタック・規約

- バンドラー・フレームワーク不使用。素のHTML/CSS/JS（ESモジュールも使わない、`<script src>`で読み込むだけ）
- APIは同一オリジンの`/memos`をfetchで叩く（`server.js`が`/`と`/app.js`を静的配信している）
- 状態はDOM上にしか持たない。フロント側で独自の状態管理ライブラリ・仮想DOMは導入しない
