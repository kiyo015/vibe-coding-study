# バイブコーディング実践習得 2週間計画

対象: WEBアプリ/ブラウザゲーム バイブコーディング経験者。CLAUDE.md・スキル・サブエージェント・hooks・MCP未理解 → チーム開発実践レベル到達目標。

使い方: 毎日「学習履歴」欄埋める。完了後メール送付、または毎日追記し都度送付。1日8h目安（Day1のみ4h前提）。時間配分目安 = インプット2h + ハンズオン5h + 振り返り1h。平日のみ実施、土日休み（実施期間 約3.5週間）。

**各日の進め方の型（8h確保のための必須ステップ）:** 以下の各項目に記載の「インプット」「実践課題」は目次に過ぎない。実施時は必ず4段で深堀りする。
1. 仕組み・概念の詳細解説（読み込みタイミング・優先順位・内部動作・コストとのトレードオフ等、公式ドキュメント相当の深さまで）
2. 実例分析（`game`・`equipment-management`など既存資産を教材に、良い点/欠けている点を具体的に指摘）
3. ベストプラクティス/アンチパターンの体系化（表形式で整理）
4. 実プロジェクト（`vibe-practice`等）への適用実践（判断練習を含む）

Day7・Day14（振り返り日）は新規インプットが元々少ない設計。8hはその週のトピックを実プロジェクトへ本格導入する**実装作業**で埋める。

---

## Day1 (2026-08-31 月) — Claude Code全体像 + CLAUDE.md基礎

**目標:** CLAUDE.md役割・置き場所・効果理解

**インプット:**
- Claude Code公式ドキュメント「CLAUDE.md」「Memory」該当ページ
- CLAUDE.md階層（プロジェクト直下 / サブディレクトリ / ユーザーグローバル）優先順位
- 書くべき内容 vs 書くべきでない内容（規約 vs 一時情報）

**実践課題:**
- 既存バイブコーディング製作物（WEBアプリ/ブラウザゲーム）1つ選定
- プロジェクト直下にCLAUDE.md新規作成。ビルド/テストコマンド・ディレクトリ構造・規約記述
- `/init` 自動生成版と比較

（実施メモ: 既存2案件game・equipment-managementは参考のみに留め、Study配下に練習用プロジェクト`vibe-practice`を新規作成。Node標準機能のみのメモAPI＋テスト4件。`init`スキルで自動生成→内容確認→日本語化して確定。追加で仕組みの詳細（読み込みタイミング・階層優先順位・コンテキストコスト）、`game/CLAUDE.md`の実例分析、ベストプラクティス/アンチパターン表、`game`の「新しい発見の扱い」「報告形式」セクションを`vibe-practice`へ輸入する実践まで実施）

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - CLAUDE.mdは「コードから読み取れない・毎回説明してる情報」だけ書く。ファイル一覧や関数シグネチャ、一般論は書かない
  - CLAUDE.mdは毎回のやりとりに乗る固定コスト。長すぎると指示追従率が下がる（lost in the middle）
  - 「機械的に検出・強制できない暗黙知」を書く場所。lintで縛れることはlintに任せる
  - 実プロジェクト（game/CLAUDE.md）は「コマンド」「アーキテクチャ説明」が丸ごと欠けていた——理想と実物のギャップを実例で確認
  - 良いパターンの輸入判断基準: 誰が触るか・レビューコストがあるか（普遍的な判断基準は輸入コストが低い）
- つまずき:
- 気づき:
- 次アクション: Day2でモノレポ的な複数ディレクトリ構成を想定し、サブディレクトリスコープのCLAUDE.mdを試す（`game`が実は4プロジェクト同居のモノレポだった件を教材にできる）
- [x] 完了

---

## Day2 (2026-09-01 火) — CLAUDE.md実践・スコープ設計

**目標:** モノレポ/複数ディレクトリでのCLAUDE.md使い分け理解

**インプット:**
- ディレクトリスコープ付きCLAUDE.md（サブディレクトリ配置時の適用範囲）
- import構文（他ファイル参照）有無調査
- 書きすぎ弊害（コンテキスト圧迫）

**実践課題:**
- Day1プロジェクト複数モジュール構成なら、サブディレクトリに専用CLAUDE.md追加
- 内容「困った時読み返すレベル」まで削ぎ落とし（不要記述削除）

（実施メモ: `game`配下4プロジェクトのnpm scriptsを比較し「なぜ直下CLAUDE.mdにコマンドが書けなかったか」を実証。`vibe-practice`に`frontend/`（素のHTML/JS、バンドラー不使用）を追加してモノレポ構成を再現、専用CLAUDE.md作成。`server.js`に静的配信追加、ブラウザで追加/削除を実機確認）

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - サブディレクトリCLAUDE.mdは「上書き」でなく直下と「合成」される。直下=方針、サブ=技術詳細という役割分担が実態に近い
  - 4プロジェクトのnpm scripts比較で「共通コマンドはdevのみ、build/test/lintは全部違う」ことが判明——直下に書けない理由が具体的コマンドの不一致として可視化できた
  - import構文は「1プロジェクト内で規約が肥大化した時」用。プロジェクト間の棲み分けには不要（サブディレクトリ分割で足りる）
  - 合成は「上書き」でなく追記。特別なマージ機構はなく、モデルが具体的な指示を優先する性質の応用
  - 実証実験: `frontend/CLAUDE.md`に検証用ルールを仕込み、ルールを知らないサブエージェントに作業依頼→自らルールの存在・適用条件に言及→サブディレクトリCLAUDE.mdが自動読込されることを実機で確認
- つまずき:
- 気づき:
- 次アクション: Day3でスキル(Skills)概要。SKILL.mdのdescription精度がトリガー精度に直結する点を意識する
- [x] 完了

---

## Day3 (2026-09-02 水) — スキル(Skills)概要

**目標:** スキルとは何か・カスタムコマンドとの違い理解

**インプット:**
- スキル仕組み（SKILL.md構成・description・トリガー方式）
- 既存公式/プラグインスキル数個、実際呼び出し挙動観察
- スキル vs サブエージェント使い分け方針

**実践課題:**
- 手元環境スキル一覧確認、3つ試用
- 自分の作業で「毎回同じ手順」1つ洗い出し（翌日自作スキル題材）

（実施メモ: `ListSkills`実行→claude.ai系とClaude Code系でスキル登録経路が別と判明。`docx`/`pptx`等のdescription作り込みを`using-superpowers`と比較し「誤発動リスクに比例した詳細度」の原則を導出。`code-review`スキルを`vibe-practice`に実行→3件のPLAUSIBLE指摘、修正、コミット`7e8698c`）

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - スキルはトリガーされるまで本文が読み込まれない。常駐するのはdescription1行だけ——CLAUDE.mdとの決定的な違い
  - description作り込み度は隣接スキルとの誤発動リスクに比例させる（似た者同士が多い領域ほど否定条件を厚く）
  - 実際にスキルをコードに対して実行すると、CLAUDE.mdの厳密なルール（マジックナンバー禁止）ほど実運用でグレーゾーンが生まれやすい
  - `.claude/plugins`配下の実ファイルを読んで実装2パターンを確認: 正式なSKILL.md形式（frontmatter2行+本文296行、常駐コストゼロを実測）と、プラグインのcommands/形式（`code-review`の実体はこちら）。`Skill`ツールからは区別がつかない
- つまずき:
- 気づき:
- 次アクション: Day4で自作スキル作成。今回洗い出した「毎回同じ手順」を題材にする
- [x] 完了

---

## Day4 (2026-09-03 木) — 自作スキル作成

**目標:** 自分専用スキル1つ作り切る

**インプット:**
- スキル作成支援機能（あれば活用）
- SKILL.md記述ルール（description精度がトリガー精度直結）

**実践課題:**
- Day3洗い出し定型作業をスキル化
- 実プロジェクトで呼び出しテスト、description 2〜3回チューニング

（実施メモ: `skill-creator`スキル使用。フル評価ループ（サブエージェント並列テスト・ベンチマーク）は個人用軽量スキルには過剰と判断し省略、軽量版で実施。日々のクローズ作業を`daily-closeout`スキル化、`Study\.claude\skills\`に配置。「今日はここまで」の発言で実際に自動発火することを確認）

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - Progressive Disclosure: メタデータ常駐→本文はトリガー時のみ→付属リソースはさらに必要時のみ、の3段階
  - descriptionはpushyに書く（undertrigger対策）が、誤発動防止の範囲限定も併記する
  - ALWAYS/NEVERの多用より「なぜ重要か」を説明する方が良いスキルになる
  - Claudeは自力でできる単純タスクにはスキルを使わない傾向。複雑・多段階なタスクほど確実に発火する
- つまずき:
- 気づき: 書いたばかりのスキルが早速、運用実態とのズレ（vibe-practiceが独立gitでなくなった前提のまま）を露呈。スキルも育てるものと実感
- 次アクション: Day5でサブエージェント設計。スキル（How）とサブエージェント（Who）の違いを実践で確認する
- [x] 完了

---

## Day5 (2026-09-04 金) — サブエージェント設計

**目標:** サブエージェント役割分担・ツール制限考え方理解

**インプット:**
- サブエージェント定義方法（frontmatter・tools制限・model指定）
- 「本体作業 vs サブエージェント委譲」判断基準
- 並列実行・isolation（worktree）概念

**実践課題:**
- 読み取り専用調査用サブエージェント（コード探索専用等）1つ自作
- 実プロジェクトで呼び出し、通常探索動作と比較

（実施メモ: `vibe-code-locator`自作→同一セッション内では使えない発見→代わりに`genshijin-investigator`と`Explore`を比較。並列実行実演（frontend/backend同時調査、副産物でserver.js無テスト発覚）。`isolation: worktree`実証実験（完了フラグ機能試作→本体無傷を`grep`で確認→半端実装のため破棄・クリーンアップ）)

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - サブエージェント定義はfrontmatter(name/description/tools/model)+ペルソナ設定の本文。スキルの「手順書」中心とは構造が違う
  - CLAUDE.mdの禁止事項は「お願い」、tools制限は「物理的に不可能にする」——強制力の質が違う
  - tools制限が「何をしていいか」、isolation:worktreeが「どこまで影響していいか」——隔離には2つの軸がある
  - サブエージェント一覧は会話開始時に固定ロード。CLAUDE.md/スキルとは違い同一セッション内での新規追加は反映されない
- つまずき: 自作`vibe-code-locator`が今セッションで使えなかった(次回セッションで要確認) → 【解決済】Day6冒頭で新セッション開始時に自動出現、動作確認済み(8,211トークン・6.6秒)。サブエージェントだけCLAUDE.md/スキルと反映タイミングが違うことが確定
- 気づき: worktreeは変更ありだと自動クリーンアップされず`.claude/worktrees/`に残る。`.gitignore`未対応だと次コミットで丸呑みするところだった
- 次アクション: Day6でhooks
- [x] 完了

---

## Day6 (2026-09-07 月) — hooks(フック)

**目標:** hooksによる自動化仕組み理解

**インプット:**
- hooksイベント種類（起動時・コマンド実行前後・ファイル編集後等）
- settings.jsonでのhooks設定方法
- 安全性考え方（自動実行コマンドのリスク）

**実践課題:**
- 簡単hook1つ設定（例: 編集後自動フォーマッタ、コミット前チェック）
- 動作確認、発火タイミングログ確認

（実施メモ: genshijinプラグインのhook実装を解読し「毎ターン見ていた文言」の生成元を特定。自作hook2つ(PostToolUse=編集後自動テスト、PreToolUse=危険コマンド事前ブロック)を作成し両方の動作を実演。stdinダンプでPre/Postの受信データ差分を実測。全ファイルの絶対パスを相対化）

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - hookの正体はstdin/stdoutのプロセス間通信。言語不問(stdin読めてstdout書ければ何でも動く)
  - PreToolUseには`tool_response`が無く、PostToolUseにはある——「事前に止められる/事後に全部見える」の技術的裏付け
  - hookは`transcript_path`(会話ログ全体)まで受け取る。強力だが、他人のhookを信頼するのは任意コード実行リスクそのもの
  - hooksはCLAUDE.md/スキルと同じく同一セッション内で即時反映。サブエージェントだけが例外
  - 4機構の整理: CLAUDE.md=常駐設定 / スキル=手順(How) / サブエージェント=ワーカー(Who) / hooks=モデルを介さないコード実行
- つまずき:
- 気づき:
- 次アクション: Day7で週次振り返り＋別プロジェクトへの統合導入
- [x] 完了

---

## Day7 (2026-09-08 火) — 週次振り返り + ミニ実践

**目標:** Day1-6知識を1プロジェクトに統合

**インプット:** なし（復習のみ）

**実践課題:**
- 別プロジェクト（ブラウザゲーム側等）選定、CLAUDE.md + 自作スキル + hooksゼロから導入
- 1週間分学習履歴読み返し、理解浅い箇所洗い出し

（実施メモ: 未実践要素5つを洗い出し、うち2つ(import構文・スキルのreferences/)を今日埋めた。新規プロジェクト`mini-game`(数当てゲーム)を作り4機構をゼロ導入。統合検証として「難易度を上げて」をサブエージェントに投げ、スキル発火・references読込・import経由の規約適用を盲検で確認。バランス変更は`game-playtester`で8戦略×1万回シミュレーション検証）

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - CLAUDE.mdのimport構文(`@path`)は実際に機能する。盲検で、importされたdocs/の内容をサブエージェントが正確に把握していた
  - スキルの`references/`は本文の指示通り「必要な時だけ」読まれる。Progressive Disclosureの3段階目が実地で機能
  - **worktree isolationはgit管理下のファイルにしか効かない**。未追跡だと隔離が素通しになる——Day1の「バージョン管理外は安全機構が効かない」と同じ根っこ
  - 同じ人が作る別プロジェクトでも規約は違っていい(vibe-practice=ESモジュール不使用 / mini-game=使用)
  - Day5の結論「サブエージェントは次回セッションから」は言い過ぎだった。正しくは「数ターン遅れて反映される」
- つまずき: 作ったゲームがユーザーの手元で動かなかった。テスト6件パス・サーバー経由の確認済みだったが、HTMLを直接開くとESモジュールのCORS制約で無言で壊れる。**テストが通ることと使えることは別問題**
- 気づき:
  - 勝敗を左右するのは「中央を狙う正確さ」でなく「フィードバックを反映する正確さ」だとシミュレーションで判明。UI設計への示唆になった
  - 不具合検出は代理指標(プロトコル判定)でなく症状そのもの(モジュールが実行されたか)を見る方が確実。Day1の「新しい発見の多くは錯覚」と同じ構図
- 次アクション（week2持ち越し）: 未実践3つ(model指定の効果検証・hooksの他イベント・グローバルCLAUDE.md)。Day8はMCP概要
- [x] 完了

---

## Day8 (2026-09-09 水) — MCP概要

**目標:** MCP（外部ツール連携仕組み）理解

**インプット:**
- MCPサーバーとは何か・何ができるか（外部API/DB/ブラウザ操作等）
- MCPサーバー接続方法・設定ファイル場所

**実践課題:**
- 使える状態のMCPサーバー1つ選定、実際呼び出し
- 呼び出し結果とツール定義照らし合わせ理解

（実施メモ: `.claude.json`(57KB)を追跡しMCP設定の実物を特定、供給経路が3つあることを解明。手元のMCPツールで正常系・異常系を実測。実践としてMCPサーバーを自作——外部SDK不使用の素のJSON-RPC実装で、手でプロトコルを叩いて3段階を検証。`.mcp.json`に設定を記述）

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - MCPは「N×M問題」をN+Mにする共通規格。Tools/Resources/Promptsの3種を公開できる
  - 接続は`initialize`→`tools/list`→`tools/call`の3段階。`tools/list`で返す定義がそのままモデルに見える
  - MCPサーバーの供給経路は3つ（ローカル設定/claude.aiコネクタ/アプリ組み込み）。設定が空でもツールが使えるのはこのため
  - **hooksとMCPは実装が似て役割が正反対** — どちらも外部プロセスをstdioで動かすが、hooksは強制発火、MCPはモデルの裁量
  - MCP固有のリスクは「外部データの持ち込み口」であること。持ち込まれた文言は指示でなくデータとして扱う
  - **MCPツールは遅延ロードされず常時コンテキストに載る**（スキルとの決定的な差）。ToolSearchはそのコストへのクライアント側の対処。自作時はツールを詰め込みすぎないこと
  - **`inputSchema`は自動検証されない**。booleanと宣言した所に文字列を渡しても素通りする。パスやコマンドを受け取るなら必ず自前で検証する
  - stdioではstdoutが通信路そのもの。`console.log`1行で通信が壊れる。デバッグはstderrへ
  - 「ツールが無い」はプロトコルエラー、「実行に失敗」は`result`内の`isError`。モデルに読ませたい失敗は`result`側に入れる
- つまずき:
- 気づき: サーバー実装で`process.cwd()`に依存すると危うい。クライアントがどのcwdで起動するか保証がないので`__dirname`基準にすべき——Day6の絶対パス排除と同じ考え方
- 次アクション: Day9冒頭で自作サーバーの接続確認（このセッションでは未接続、Day5のサブエージェントと同じパターン）
- [x] 完了

---

## Day9 (2026-09-10 木) — MCP活用実践

**目標:** 自分の開発フローにMCP組み込む

**インプット:**
- 用途別MCPサーバー種類（ブラウザ操作・DB・外部SaaS等）
- 権限設定・信頼境界考え方（外部データは指示でなくデータ扱い原則）

**実践課題:**
- 自分のプロジェクト関連MCPサーバー探索、1つ接続
- 実開発作業（デバッグ・データ確認等）で使用

（実施メモ: Day8の持ち越し(自作サーバー接続)を確認。信頼境界の実証実験——計画シートに4種のインジェクション文言を仕込み、MCPツール経由でコンテキストに流し込んで防御を検証、全パターン不発。実装に外部データの区切りを追加し、`search_learnings`ツールを追加して学習記録の横断検索を可能にした）

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - MCPサーバーの反映は「次回セッション」。子プロセスの起動を伴うため、ファイルを読み直せば済む他機構とは性質が違う
  - **2種類のリスクは別物**: サーバー自体の悪意(コード実行)と、サーバーが運ぶデータの悪意(インジェクション)。後者は**サーバーを監査しても防げない**
  - インジェクションの手口4種(素直な命令/権威の詐称/緊急性の演出/正常データへの紛れ込み)のうち、**紛れ込みが最も巧妙**。正当なタスク名の括弧書きに混ぜられると異物に見えない
  - 防御は3層(モデルの原則/権限設計/データ設計)。**最も確実なのは権限設計** — 「騙されない」に賭けるより「騙されても実害が出ない」方が強い(Day5のtools制限)
  - 外部データの区切りは攻撃を防がない(区切り自体を偽装できる)。価値は「設計者が外部データを意識せざるを得なくなる」点にある
- つまずき: インジェクション文言の書き込みが分類器にブロックされた。多層防御が機能している実例でもあった
- 気づき: ツールを増やした時、エラーメッセージを使い回していた(「集計に失敗」が検索ツールにも出る)。増築時は既存の共通処理も見直す必要がある
- 次アクション: Day10でプラグイン化。ここまでの資産(スキル2・サブエージェント2・hooks2・MCP1)を1つにまとめる
- [x] 完了

---

## Day10 (2026-09-11 金) — プラグイン化

**目標:** スキル・エージェント・hooksまとめ配布可能形理解

**インプット:**
- プラグイン構成（マニフェスト・スキル/エージェント/hooks同梱方法）
- ローカルプラグイン作成〜有効化流れ

**実践課題:**
- Day4〜6作成スキル・サブエージェント・hook、1プラグインにまとめ
- 別プロジェクトで有効化、動作確認

（実施メモ: 資産を棚卸しし「全部まとめても別プロジェクトで動かない」と判断、汎用的なhooks2つだけを`dev-guard`プラグインに。`post-edit-test`はpackage.json自動検出に汎用化。`--scope project`で有効化し、`claude plugin details`で5機構のコンテキストコストを数値で確認。後始末で絶対パスの自動書き込み・二重発火・過去記録のリンク切れを解決し、成果物リンクをタグ方式に移行）

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - **プラグイン化は「まとめる作業」でなく「汎用化する作業」**。プロジェクト専用の資産を詰めても配布先で動かない
  - YAGNIで見送った汎用化(Day7のpackage.json自動検出)が、要件が変わった瞬間に必要になった。YAGNIは「今やらない」であって「永遠にやらない」ではない
  - `claude plugin`の`--scope`はデフォルトが`user`(全プロジェクト)。明示しないとグローバル設定に書き込まれる
  - **5機構のコンテキストコストが数値で揃った**: CLAUDE.md=全文常駐 / スキル=常駐40・発火920tok(本文は常駐の23倍) / サブエージェント=常駐40・発火200tok / MCP=定義全部 / **hooks=ゼロ**
  - プラグインはキャッシュ(`~/.claude/plugins/cache/`)にコピーされて実行される。ソースを直しても`update`しないと反映されない
- つまずき:
  - 単体検証で出力が空に。原因はhook本体でなくGit Bashの`printf`でWindowsパスが崩れたテスト入力の方だった。**テストが失敗しても本体は正しいこともある**(Day7の逆パターン)
  - 異常系検証で`MAX_TRIES`を変えてもテストが落ちなかった。テストが定数を参照していたため——結果的に良いテスト設計の実証になった
- 気づき:
  - ツールが自動生成した設定に絶対パスが入っていた。自動生成物も中身を確認すべき
  - `master`参照のリンクは、リンク先の更新・削除で壊れる。しかも最初から「その日時点」を指せていなかった。タグなら事前に書けて永続的に固定できる
  - スキルにモデル名を固定で書いていて古くなった。Day1の「すぐ陳腐化する情報を書くな」と同じ構図
- 次アクション: Day11冒頭でプラグイン経由のhooksが発火するか確認(セッション起動時に読み込まれるため今日は未検証)。その後チーム運用設計
- [x] 完了

---

## Day11 (2026-09-14 月) — チーム運用設計

**目標:** チーム開発でのバイブコーディング運用ルール設計

**インプット:**
- 共有CLAUDE.md/プラグインのリポジトリ管理方法（バージョン管理下運用）
- コードレビューへのAI活用方針（レビュー用スキル・チェックリスト運用）
- 権限設定（permission mode）チーム標準化

**実践課題:**
- チーム向けCLAUDE.md雛形作成（規約・禁止事項・レビュー基準明記）
- コードレビュー用スキル/フロー1つ設計・試用

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - チーム共有前に「自分のPCでたまたま動いている」前提を探す。`settings.local.json`は個人のgit除外設定で隠れていただけで、リポジトリの`.gitignore`に無かった
  - **信頼を承認していないフォルダでは、プロジェクト設定の`permissions.allow`が無視される**。cloneしたリポジトリが自分に権限を与えられないための仕様
  - ルールは「強制(設定・hooks・tools)」と「お願い(CLAUDE.mdに書くだけ)」を区別して書く。雛形`templates/team-CLAUDE.md`に反映
  - denyルールは`&&`で分解後に**先頭一致しか見ない**。フックの正規表現とは漏れ方が違うが、どちらもブロックリストなので穴はゼロにできない
  - **PowerShellツールにはフックもdenyルールも効いていなかった**(matcherが`Bash`だけ)。対象ツールを増やす時は、そのツールに効いているガードを先に実測する
  - レビュー役`team-reviewer`は`tools: Read, Grep, Glob`で読み取り専用を保証。差分と意図だけ渡し、自分の見立ては渡さない
  - レビュー指摘は実測してから直す・反論する。1回目は1件外れ(孫プロセスの件)、2回目は15例すべて的中。**自分で書いたテスト31件が通っていても、自分のコミットを止める誤検知が残っていた**
- つまずき:
- 気づき:
- 次アクション: Day12冒頭でプラグイン1.0.3が有効か確認(PowerShellツールでも止まるか)。`claude` CLIに再ログインしてから、`claude -p`によるヘッドレス実行・定期実行に進む
- [x] 完了

---

## Day12 (2026-09-15 火) — 自動化・定期実行

**目標:** 定期実行・繰り返しタスク自動化理解

**インプット:**
- スケジュール実行仕組み（定期タスク・cron的運用）
- 繰り返し実行（loop的運用）との違い・使い分け

**実践課題:**
- 定型チェック（依存パッケージ更新確認・簡易ヘルスチェック等）1つ定期実行設定
- 1回発火、結果確認

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - `claude -p`が自動化の土台。`--output-format json`で`result`・終了コード・`total_cost_usd`・`permission_denials`が機械で読める
  - **コストの正体は指示の長さでなく、毎回積み直す文脈**(CLAUDE.md・スキル・ツール定義)。`2+3`だけで$0.075。haikuで$0.034、MCP除外は今日は効果なし(元々読んでいないため)——**対策は測ってから決める**
  - **無人実行で最初に詰まるのは権限。** 要約を頼んだのに自分で実行しようとして確認待ちで停止した。対策は`--tools ""`(道具を与えない)と許可ルールの事前登録
  - 定期実行の設計で効くのは**AIを呼ぶ回数**。事実の収集は決定的な処理、AIは異常時だけ→正常時コスト0(月$0.03 vs 毎回呼ぶ月$1.65)
  - 「異常」の定義は放置したら損害が出るかで決める。未pushは異常、未コミットは情報。**毎日鳴る警告は誰も見なくなる**
  - `/loop`は前回の記憶がある(同じセッション)・セッションと共に消える。定期タスクは毎回まっさら・ディスクに残る。指示文は単体で完結させる必要がある
- つまずき:
  - アプリの定期タスクが権限の確認待ちで20分以上停止。承認後は記憶され、2回目は無人で完走
  - `.cmd`に日本語コメントを書いたらcmd.exeがShift-JISとして読んで壊れ`exit=9009`。`chcp 65001`でも直らず、コメントを英数字にして解決
  - タスクから呼ぶバッチはカレントディレクトリが想定と違う。`%~dp0`基準に修正
- 気づき:
  - Day10で測った常時コストが、定期実行では1回ごとの請求として跳ね返る。コンテキスト設計＝コスト設計
  - 未pushの状態を意図的に作り、検知されること・pushで消えることの両方を確認した。「いつも緑」「いつも赤」は見られなくなる
- 次アクション: Day13の総合演習へ。今日までに揃った道具(ガード・レビュー役・健康診断・定期実行)を1つの開発の流れに組み合わせる
- [x] 完了

---

## Day13 (2026-09-16 水) — 総合演習

**目標:** 学習要素全部（CLAUDE.md/スキル/サブエージェント/hooks/MCP/プラグイン）1プロジェクト統合

**インプット:** なし（総合実践）

**実践課題:**
- 新規 or 既存プロジェクトに「バイブコーディング環境」一式構築
- 機能追加1つ、構築環境のみで最初から最後まで実施

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
  - mini-gameに難易度選択を追加し、CLAUDE.md・スキル・サブエージェント2種・プラグイン・健康診断・MCPを1つの開発の流れで全部通した(要件→仕様書→計画→TDD実装→測定→レビュー→確認)
  - **フックとTDDは噛み合う。** テストを先に書くとdev-guardが差し戻し、その出力がRED確認そのものになった
  - **数値目標に合わせに行くと運ゲーか過剰適合に落ちる。** 手数削りは情報理論上の限界(1〜1000を9回では2⁹=512通りしか区別できない)を割り、スキルの資料が止めた。難易度の軸を「回数」と「ヒントの粒度」の2つにし、93%/84%/51%の階段にした。外した理由は文書に残す
  - スキルの資料の経験則「最悪手数ちょうどではほぼ負ける」は実測(8〜9割勝てる)と食い違っていた。道具の中の知識も実測で更新する
  - **レビューで直したのはコードより文書の陳腐化が多かった。** 仕様書・テストのコメント・自分の安全宣言が数時間で嘘になっていた
  - 健康診断のAI要約が壊れていた原因は2段: cmd.exe経由で引数のプロンプトが改行で切れ`--tools ""`も消えていた(Day12のツール禁止は一度も効いていなかった)／状態の要約は基準を添えないと失敗に見えない。標準入力と基準の明記で解決
  - テストは「狙った理由で落ちる」ことまで確かめる。読み込み時に本体が`process.exit`して偽りの成功になっていた
- つまずき:
- 気づき:
- 次アクション: Day14で14日分の記録を読み返し、要点サマリと「まだ弱い」領域3つ・次の学習テーマを決める。`search_learnings`で横断検索する
- [x] 完了

---

## Day14 (2026-09-17 木) — 総振り返り + 今後ロードマップ

**目標:** 2週間学習定着、次学習テーマ決定

**インプット:** なし

**実践課題:**
- 14日分学習履歴読み返し、要点サマリ化
- 「まだ弱い」領域3つ特定、次月学習テーマ書き出し

**学習履歴記入欄:**
- 実施時間:
- 学んだこと:
- つまずき:
- 気づき:
- 次の学習テーマ:
- [ ] 完了

---

## 2週間総括（Day14完了後記入）

- 達成できたこと:
- 未達成・積み残し:
- チーム開発で使えそうな武器:
- 次月テーマ:
