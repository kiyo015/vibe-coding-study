{
  "name": "dev-guard",
  "version": "1.2.0",
  "description": "開発中の安全装置。Bash・PowerShellで取り返しのつかないコマンド(force push・reset --hard・rm -rf・dotnet ef database drop・dotnet ef migrations remove・SQLのDROP/TRUNCATE)を実行前にブロックし、コードファイルの編集後にはそのファイルが属するプロジェクトのテストを自動実行する(JavaScript/TypeScriptはnpm test、C#は対応する.Testsプロジェクトのdotnet test)。",
  "author": {
    "name": "kiyo015"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/pre-bash-guard.js\"",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/post-edit-test.js\"",
            "timeout": 90
          }
        ]
      }
    ]
  }
}
