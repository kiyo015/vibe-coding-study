# コーディング規約

- 外部依存を追加しない。Node標準機能とブラウザ標準APIだけで完結させる
- ゲームロジック（`src/`）はDOMに触れない。`document`や`window`を参照した時点で設計が壊れているとみなす
- UI層（`web/`）にゲームのルール判定を書かない。勝敗・残り回数の判断は必ず`src/game.js`側に置く
- マジックナンバーを使わない。`MAX_TRIES`のように名前付き定数として`src/game.js`からexportする
- 乱数・時刻など非決定的な要素は引数で注入できる形にする（テストで固定値を渡せるようにするため）
